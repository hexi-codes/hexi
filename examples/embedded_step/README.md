# embedded_step

Direct Python embedding of Hexi core + adapters.

## Setup
```bash
pip install -e ../..
```

## Run
```bash
python run_embedded.py "Add a failing test for X"
```

This project bypasses CLI and directly constructs `RunStepService`.
