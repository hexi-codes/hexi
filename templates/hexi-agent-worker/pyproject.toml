[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sample-hexi-worker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["hexi"]

[project.optional-dependencies]
dev = ["pytest>=8,<9"]
